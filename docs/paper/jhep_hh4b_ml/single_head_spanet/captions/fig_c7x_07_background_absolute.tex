Absolute selected-background yields. The stacked linear axis starts at zero; total-yield bars are asymmetric 68\% source-member intervals. Transferred QCD is the primary exactly-$3b$ to $\geq4b$ prediction.
