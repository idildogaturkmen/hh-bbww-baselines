Support-qualified systematics-aware Asimov scan. Markers are supported central points; source-member bootstrap 68\% bands are drawn only when at least 80\% of replicas pass the frozen support contract. Crosses denote unsupported points and no line or band is interpolated across them. The lower panel shows transferred exactly-$3b$ rows (solid) and direct $\geq4b$ QCD closure rows (dashed).
