The strongest nominal baseline is the BDT, but nominal ordering must not be interpreted as a publication-model choice because the frozen multijet uncertainty reduces all systematic-aware sensitivities to approximately zero.
