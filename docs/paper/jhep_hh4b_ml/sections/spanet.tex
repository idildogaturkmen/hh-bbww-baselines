Single-head and joint two-head permutation-symmetric assignment studies require unambiguous truth-matched signal labels. Background assignment loss is masked; classification loss may use all supported train events.
