All resampleable frozen-model performance values use one common 1000-replica source-member bootstrap registry. Primary intervals are asymmetric 16th--84th percentile ranges; model differences are paired replica by replica. Invalid sparse-closure replicas remain undefined and are never replaced by zero.
