# HH→4b one-time validation campaign pre-submission freeze

The 116-source package and independent audit are frozen here before the only authorized `condor_submit` call. Every source requires an exclusive durable marker before access; automatic reruns are forbidden; test remains sealed.
