# Global BDT corrected full-budget pilot

Corrected operational pilot cluster `3798575` passed scheduler, package,
payload-order, nested-selection, prediction, and sealed-data audits. It is
permanently `NEVER_RESUBMIT` and excluded from final scientific aggregation.

The measured workload supports one job per outer-fold/variant: 573.5 seconds
worker walltime, 2.31 GiB MaxRSS, and about 1.93 GiB peak scratch use.
