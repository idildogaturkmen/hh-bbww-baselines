# HH→4b publication train only checkpoint

This checkpoint was frozen before validation. The nominal cut is unchanged, validation and test payload counts are zero, and no official CMS status is claimed.
