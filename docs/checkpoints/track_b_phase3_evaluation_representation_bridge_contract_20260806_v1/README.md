# Track B Phase 3 evaluation and representation-bridge contract

This checkpoint freezes the division of responsibilities between the two
research tracks.

## Track A

Track A is the primary training and physics-performance lane. It is currently
at cut-baseline establishment. ParT and JP-JEPA integration is intentionally
deferred until the common dataset/split/label/metric contract and a native
SPA-Net baseline are frozen.

## Track B

Track B is the external provenance, released-Sophon reference, control-sample,
and later domain-stress lane. It does not presently establish a compatible
jet-clustering and truth-assignment contract for direct SPA-Net training.

## Asset inventory interpretation

The 698 discovered rows are broad lexical candidates and references. They are
not 698 validated model implementations or checkpoints. The checkpoint stores
a classified review so later implementation work starts from targeted,
interface-level validation rather than the raw lexical count.

## Next gate

`40J7C_review_and_commit_phase3_evaluation_representation_bridge_contract`
