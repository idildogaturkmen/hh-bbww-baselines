# HH4b ttbar8 canary retry2 contract

This checkpoint freezes a second reconstruction-only retry for
`ttbar_100k_shard003`.

Retry2 reuses the immutable 10,000-entry ROOT generated by job `3654710.0`.
It does not rerun MadGraph, Pythia, or Delphes.

The contract fixes two technical defects observed in retry1:

1. A portable Python 3.9 environment is transferred and placed first on
   `PYTHONPATH`.
2. The complete access-point return-directory hierarchy is created before
   submission.

The submit description queues exactly one job but has not been submitted.
It contains neither `hold = True` nor `requirements = False`.

The failed retry1 job `3654939.0` remains held until the next submission
gate.
