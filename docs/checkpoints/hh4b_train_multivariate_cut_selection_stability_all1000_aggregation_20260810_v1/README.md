# HH→4b all1000 checkpoint

This checkpoint was frozen before validation. The nominal cut is unchanged, validation and test payload counts are zero, and no official CMS status is claimed.

For the all-1000 role, the ten large ranked-result shards remain in the committed source artifact and are bound here by exact path, byte count, and SHA-256. Every downstream-consumed table and the independent byte-identical-rerun audit are copied into this compact checkpoint.
