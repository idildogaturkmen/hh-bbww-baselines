# Waiting on signal normalization confirmation

Sent Harvey a short update on the HH→4b Delphes baseline and asked whether to:

1. Normalize generated signal samples to official SM HH cross sections × BR(H→bb)^2, or
2. Regenerate signal samples with a different process definition/card.

Current status:
- Background production is sufficient for steering decisions.
- QCD bbbb HT-sliced sample is the nominal QCD model.
- Inclusive ttbar is the nominal top background.
- Zbbbb is included as a nominal background.
- ttbb 50k is kept diagnostic only to avoid possible double-counting with inclusive ttbar shower heavy flavor.
- First BDT baseline is diagnostic; not a final sensitivity result.

Do not scale signal production until signal normalization convention is confirmed.
