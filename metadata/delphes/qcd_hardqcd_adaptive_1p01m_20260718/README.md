# Canonical adaptive physical-QCD 1.01M registry

This registry combines:

- Wave 1: 500,000 events
- Wave 2: 500,000 canonical events
- sealed bin-3 test fill: 10,000 events

Total: 1,010,000 generated inclusive-HardQCD events.

The Wave 2 recovery replaces one missing shard with exactly the same
bin, seed, shard ID, event count, and split. It does not add duplicate
statistical events.

This sample is pending a combined stratum-level weighted statistical
audit. No additional generation and no 5M campaign are authorized by
this registry.

The older separate 1M QCD anchor is excluded until generator,
detector-card, reconstruction, and normalization compatibility are
demonstrated.
